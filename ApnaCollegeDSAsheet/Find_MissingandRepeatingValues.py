
# HashMap:
# what is Hashmap?
# It is a kind of data structure which is very fast,used in every large code base.
# Map is used in key value pair. like:  map<node,m> a; or map<array,m> a;

# Set:
# Set is container which contains unique value in ordered state. There is no indexoing set. Elements sre identified by their own values.
# Once a value is inserted it can not be modified in a set. Set has dynamic size, which means we dont need to declare its size.
# But it consumes more memory than array, not suitable for large datasets.


